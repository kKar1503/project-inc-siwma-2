import { Chip } from '@mui/material';

const NegotiableBadge = () => (
  <Chip
    style={{
      color: '#FFFFFF',
      fontWeight: 'bold',
      marginRight: 5,
    }}
    color="warning"
    label="Negotiable"
  />
);

export default NegotiableBadge;
