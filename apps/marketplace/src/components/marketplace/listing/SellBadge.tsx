import { Chip } from '@mui/material';

const SellBadge = () => (
  <Chip
    sx={{
      backgroundColor: '#F86262',
      color: '#FFFFFF',
      fontWeight: 'bold',
      mr: 0.5,
    }}
    label="Selling"
  />
);

export default SellBadge;
