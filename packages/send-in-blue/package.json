{
  "name": "@inc/send-in-blue",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpx rimraf dist && tsc && node esbuild.config.js",
    "build:prod": "rm -rf dist && node esbuild.config.js",
    "clean": "pnpx rimraf dist",
    "dev": "pnpx rimraf dist && tsc -w",
    "lint": "eslint src/** --ext .ts --parser-options project:\"./tsconfig.json\" --ignore-pattern \"src/**/*.html\""
  },
  "dependencies": {
    "@inc/db": "workspace:*",
    "@inc/errors": "workspace:*",
    "sib-api-v3-sdk": "^8.5.0"
  },
  "devDependencies": {
    "@inc/eslint-config-custom": "workspace:*",
    "@types/sib-api-v3-sdk": "workspace:*"
  }
}
