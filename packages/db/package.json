{
  "name": "@inc/db",
  "version": "0.0.0",
  "main": "index.ts",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:seed": "prisma db seed"
  },
  "prisma": {
    "seed": "ts-node prisma/seeding/seed.ts --transpile-only"
  },
  "dependencies": {
    "@prisma/client": "^4.11.0"
  },
  "devDependencies": {
    "prisma": "^4.12.0"
  }
}
