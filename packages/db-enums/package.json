{
  "name": "@inc/db-enums",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "copy": "tsc copyEnums.ts && node copyEnums.js && pnpm build",
    "build": "pnpx rimraf dist && tsc && node esbuild.config.js",
    "build:prod": "pnpx rimraf dist && node esbuild.config.js"
  },
  "devDependencies": {
    "@types/node": "18.15.3",
    "ts-node": "^10.9.1"
  }
}
